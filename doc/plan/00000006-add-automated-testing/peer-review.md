# Peer Review — Add Automated Testing

## Summary

The branch delivers the deferred general-cleanup finding #4: pytest tooling (pinned in the dev container at 9.1.1, configured in `pyproject.toml`), a self-contained package suite under `tests/` built on a Nim fixture game, and a separate example suite under `examples/*/tests/` that doubles as consumer documentation — 58 tests in total, plus the story's new `automated-testing.md` artifact and CONTRIBUTING/README updates. **pyright: 0 errors, 0 warnings. ruff: all checks passed. pytest: 58 passed.** Every item on the story's coverage list is implemented, the seven plan steps map one-to-one to commits in plan order with the step name as the commit message, and the plan includes the required README-check step (executed; one sentence added). The sign-convention assertions were checked against the game theory of the fixture (forced-line parity, pile-of-2 win-in-1) and are correct. One commit (`.gitignore`: ignore `.claude/settings.local.json`) is outside both story and plan — added at the developer's explicit request mid-branch; recorded below for traceability. No Critical or Major issues; six Minor comments.

## Comments

### Minor

| # | Status | Resolution | Location | Comment | Suggested Change | Code Snippet |
|---|--------|------------|----------|---------|-----------------|--------------|
| 1 | Resolved | Added `examples/tictactoe_learning/tests/test_nn_evaluator.py`: perspective encoding negates between the two players, occupied/empty squares map to ±1/0, and `decode_policy` yields a legal-ply distribution summing to 1 that ignores logits on masked (illegal) squares. | [examples/tictactoe_learning/tictactoe_nn_evaluator.py#L16-L41](../../../examples/tictactoe_learning/tictactoe_nn_evaluator.py#L16-L41) | `TicTacToeNNEvaluator` is the most sign-convention-dense example module — `encode_position` multiplies every cell by `active_player_id` so the model always sees "my pieces as +1", and `decode_policy` masks illegal squares to exactly zero — yet it is the one example module with no tests. The example suite's stated purpose is to model how a consumer tests a game implementation, and perspective encoding is precisely the kind of convention the story exists to protect (a regression here silently corrupts training data). The story's minimum list (finding #4) is met, so this is a gap beyond scope rather than a missed requirement. | Add a small test module: encoding the same board from the two players' perspectives yields negated tensors; a position mid-game encodes occupied squares as ±1 and empty as 0; `decode_policy` gives zero probability to occupied squares and a distribution summing to 1 over legal ones. | `[active * cell for cell in position.board]` |
| 2 | Resolved | Softened the comment to what is true: square 3 wins immediately, while most alternatives let P2 play 6 and complete the middle row, so the search signal for square 3 is unambiguous. | [examples/tictactoe/tests/test_mcts_integration.py#L14-L16](../../../examples/tictactoe/tests/test_mcts_integration.py#L14-L16) | The comment claims "every other ply loses to P2 completing the middle row", which overstates the position: P1 playing square 6 *blocks* the middle row and keeps the 1-2-3 threat, after which P2 must defend square 3 and the game is roughly balanced. The test's conclusion is unaffected — square 3 is an immediate certain win and clearly dominant in visits — but a factually wrong game-theory comment in a suite meant as consumer documentation undermines its teaching role. | Soften to what is true: square 3 wins immediately, while most alternatives lose to P2 playing 6, so the search signal is unambiguous. | `# every other ply loses to P2 completing the middle row, so the search` |
| 3 | Resolved | Added `torch.manual_seed(0)` before the model init in `test_training_reduces_loss_on_fixed_targets`, fixing the init (and hence the optimisation trajectory) so the pass is deterministic. | [tests/learning/test_training_loop.py#L91](../../../tests/learning/test_training_loop.py#L91) | `test_training_reduces_loss_on_fixed_targets` asserts `history[-1].total < history[0].total` after 20 Adam epochs from a *random* weight init. In practice this converges essentially always (verified over repeated runs), but it is the one test in the suite whose pass is probabilistic rather than forced, which sits uneasily with the conventions doc's "tests must not flake" pledge — an unlucky init or a future torch version change could produce a spurious failure unrelated to the code under test. | Call `torch.manual_seed(...)` at the top of the test so the init (and hence the trajectory) is fixed. This is seeding for reproducibility of an environment input, not the "seed and assert exact sequences" pattern the conventions doc warns against. | `history = loop.train(_samples(), epochs=20, ...)` |
| 4 | Resolved | `NimPosition.__init__` now raises `ValueError` unless `1 in takes`, the cheapest guarantee that no reachable non-empty pile becomes a dead end; the two configurations in use (`(1, 2)` and `(1,)`) satisfy it. | [tests/core/nim_fixture.py#L39-L48](../../../tests/core/nim_fixture.py#L39-L48) | `NimPosition` accepts take-sets that create dead-end positions: with `takes=(2,)` a pile of 1 has `outcome is None` but no legal plies — a state the framework treats as impossible (`MCTSEngine._select_best_ply` raises "position should have been treated as terminal"). The two configurations tests actually use, `(1, 2)` and `(1,)`, are safe because they include a take of 1, but the fixture is shared infrastructure that future tests will reach for, and nothing stops a dead-end configuration compiling and half-working. | Either validate in `__init__` that `1 in takes` (cheapest guarantee of no dead ends), or state the constraint in the class docstring so a future test author doesn't discover it via a confusing MCTS RuntimeError. | `takes: tuple[int, ...] = (1, 2),` |
| 5 | Resolved | Replaced the identity comprehension with `list(result.game_log)`. | [tests/core/test_standard_game.py#L69](../../../tests/core/test_standard_game.py#L69) | `[entry for entry in result.game_log]` is an identity comprehension — it only converts the sequence to a list for comparison. | `list(result.game_log) == [...]`. | `assert [entry for entry in result.game_log] == [` |
| 6 | Resolved | Noted the `.gitignore` addition under a new "After-completion notes" section in `story.md`, keeping the plan-to-commit audit trail complete. No code change needed. | [.gitignore#L13-L15](../../../.gitignore#L13-L15) | The commit ignoring `.claude/settings.local.json` (`ee222d5`) is covered by neither the story nor the implementation plan. It was added at the developer's explicit request mid-branch as too small to warrant its own story, and it is inert development tooling with no interaction with the testing work — recorded here so the plan-to-commit audit trail stays complete, not because any action is needed. | None required; optionally note the addition under the story's Out of Scope/After-completion notes if strict plan-commit correspondence is wanted. | `.claude/settings.local.json` |

---

**Findings by severity:** 6 Minor

**Notes verified as correct (no action):**

- **Plan/story/commit consistency** — the seven plan steps land as seven commits in plan order, each named after its step; the story's coverage list (MCTS terminal handling and sign conventions incl. win-in-1, visit-distribution normalisation, `SelfPlayCollector` target signs, `StandardGame` outcome conversion, `TrainingLoop` weighted epoch loss, `TicTacToePosition` outcome/legality) is fully implemented; the README-check step is present in the plan (guide requirement) and was executed.
- **Fixture game theory** — the assertions were re-derived: pile 2 with takes 1–2 is a win by taking 2; forced-line pile 3 gives plies P1-P2-P1 with player 1 winning, so target values `[+1, −1, +1]` in the collector's reverse-chronological sample order (and `[+1, −1, +1, −1]` from pile 4) are correct against `SelfPlayCollector`'s `-final_outcome` back-labelling.
- **MCTS sign assertions** — the terminal take-2 child's `average_value == -1.0` exactly (terminal outcome re-evaluated on every visit, stated from the loser's perspective); root visit accounting (`root.visits == iterations`, children sum to iterations) holds because every backpropagation path passes the root and exactly one child.
- **`TrainingLoop` frozen-model trick** — SGD with `lr=0.0` (no momentum/weight-decay) is an exact numerical no-op, so recomputing per-batch losses outside the loop is valid; the expected sample-weighted mean `Σ(meanᵢ·nᵢ)/N` matches the finding-#7 algebra, and the ragged 2/2/1 batching genuinely exercises the weighting.
- **Private-method usage** — the three tests driving `_backpropagate`, `_expand_node`, and `_choose_ply` each state why the behaviour is not publicly observable, matching the conventions doc's escape hatch; everything else goes through public API.
- **Tooling changes** — the pytest Dockerfile pin follows the ruff precedent verbatim; `testpaths` makes bare `pytest` run both suites; `tests/` added to pyright's include (and it passes); `tests/` is excluded from distribution by the existing setuptools include patterns; all four documented commands were executed as written.
- **Example-suite import convention** — example tests import the library and the example modules absolutely, satisfying TID252 ("parents") without new per-file ignores; the ruff `I001` import-sort finding that arose during development was fixed before commit.
