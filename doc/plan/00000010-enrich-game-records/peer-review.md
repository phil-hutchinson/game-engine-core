# Peer Review — enrich-game-records (issue #10)

## Summary

The branch delivers both story items: `GamePosition.outcome_reason` flowing into a new required `GameResult.result_reason` (and into the per-game tournament JSON), and the `GameLogging` protocol split out of `GameUI`, with `StandardGame` taking a required `GameLogging` plus an optional `GameUI` and `Tournament` running headless via `game_ui=None`. All six plan steps are implemented in order, in-repo implementers (TicTacToe, Nim fixture) and tests are updated per the plan, and the README was refreshed (the plan includes a dedicated README verification step, Step 6, as required). Tooling is clean: `pyright` reports 0 errors/0 warnings, `ruff check .` passes, and the full test suite passes (104 tests). No Critical or Major issues found; four Minor comments below.

## Comments

### Minor

| # | Status | Resolution | Location | Comment | Suggested Change | Code Snippet |
|---|--------|------------|----------|---------|-----------------|--------------|
| 1 | Resolved | Assert replaced with an explicit `ValueError` raise. | [game_engine_core/game/standard_game.py#L61](../../../game_engine_core/game/standard_game.py#L61) | The `outcome_reason` contract is enforced with a bare `assert`. This guards *external* code (downstream game implementations, e.g. the CtF repo), not an internal invariant — under `python -O` the assert is stripped and a contract-violating position silently puts `None` into the `str`-typed `result_reason`, later serialising as `null` in tournament JSON. An explicit exception also gives the implementer a self-diagnosing message. | Replace the assert with an explicit raise, e.g. `if result_reason is None: raise ValueError("GamePosition.outcome_reason must be non-None once outcome is set")` (or similar), keeping the narrowing for the type checker. | `assert result_reason is not None` |
| 2 | Resolved | Class docstring now notes `render_final_board` (and `render_before_ply`) only apply when a `game_ui` is supplied. | [game_engine_core/game/standard_game.py#L12-L17](../../../game_engine_core/game/standard_game.py#L12-L17) | With the new optional-UI mode, `render_final_board` is now conditional: it has no effect when `game_ui is None`. The new class docstring documents `game_logging` and `game_ui` but not this interaction, so a headless caller passing `render_final_board=True` gets silent no-op behaviour with no documented explanation. (`Tournament` correctly stopped passing it.) | Extend the docstring with one line noting `render_final_board` only applies when a `game_ui` is supplied. | `game_ui is only for interactive board display — pass None for headless play.` |
| 3 | Resolved | Trailing newline added. | [game_engine_core/models/game_result.py#L11](../../../game_engine_core/models/game_result.py#L11) | The file was modified on this branch (adding `result_reason`) but still lacks a trailing newline — the same branch fixed the identical issue in `standard_game.py`, so this one looks like an oversight. | Add a trailing newline to the file. | `game_log: Sequence[tuple[str, str]]  # (ply annotation, board after ply)` (no `\n` at EOF) |
| 4 | Resolved | Code kept as-is (explicit protocol inheritance accepted); story and implementation plan wording updated to match. | [examples/tictactoe/tictactoe_ui.py#L8-L11](../../../examples/tictactoe/tictactoe_ui.py#L8-L11) | Story and plan both describe `TicTacToeUI` conforming to the two protocols *structurally* ("structural protocols; the method sets don't collide" / "structural conformance"), but the implementation inherits both protocol classes nominally. The deviation has a defensible justification — it matches the pre-existing explicit `GameUI` subclassing and gains instantiation-time abstract-member checks — but it is an undocumented departure from both documents (note the test fixtures' `NimStubUI`/`LoggingOnly` do conform structurally, so the repo now demonstrates both styles without comment). | Either keep as-is and treat this as the accepted style (no code change), or drop the explicit bases to match the documents. If kept, consider a one-word tweak to the class docstring ("Implements both protocols…") so it doesn't imply structural-only conformance. | `class TicTacToeUI(`<br>`    GameUI[TicTacToePly, TicTacToePosition],`<br>`    GameLogging[TicTacToePly, TicTacToePosition],`<br>`):` |
