# Personal, machine-local container environment — one KEY=VALUE per line.
# Rename (or copy) this file to devcontainer.env, edit, and rebuild the
# container. devcontainer.env is gitignored; this template is not, so don't
# put personal values here.
TZ=UTC
